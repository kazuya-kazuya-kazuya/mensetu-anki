# 面接想定問答 暗記ノート

このリポジトリは、個人用の面接練習Webアプリ（https://mensetsu-anki.vercel.app/ ）の実装に利用する。

## 基本方針

- 公開対象は `index.html` / `style.css` / `app.js` / `data.js` / `videos/`。
- `data.js` は各デッキ（問答セット）の質問・回答・メモを持つデータファイル。既存デッキの内容は理由なく書き換えない。
- 新しい問答セットを追加するときは、既存デッキを壊さず `decks` 配列に追記する。
- このアプリは氏名・診断名・服薬・通院など個人の健康情報を含む。外部送信は一切行わず、進捗保存もlocalStorageのみに限定する。
- APIキー、認証情報はコミットしない。

## 構成

- `index.html`: ページ構造（デッキ選択、暗記/一覧/未回答/動画タブ）
- `style.css`: スタイル
- `app.js`: アプリ本体のロジック
- `data.js`: デッキごとの質問・回答データ
- `videos/<deckId>/<no>.mp4`: デッキに紐づく読み上げ動画（`deck.videoDir` が設定されているデッキのみ）
- `README.md`: ローカル確認・デプロイ手順
- `scripts/`: ローカル配信・品質確認用スクリプト

## 変更時の確認

1. `npm.cmd run check` — 必須ファイルの存在、`app.js`の構文、`data.js`の妥当性とデッキ/問題数を確認
2. `npm.cmd run dev` でローカル表示し、デッキ切り替え・暗記/一覧/未回答の各モードを確認
